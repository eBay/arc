<div>
    <h1>ERROR: This should not have been rendered</h1>
    <p>This file is here because, currently, Marko requires a "base" version of a file to resolve tags.</p>
    <p>(<code>site-header.marko</code> is needed even though we will only ever render <code>site-header[desktop].marko</code> or <code>site-header[mobile].marko</code>)</p>
    <p>In the future this should not be the case</p>
</div>